# Publicación — captcha + anuncios mobile

Publicada el 5 de septiembre de 2026, 22:09:13 ART (6/9, 01:09:13 UTC).
Autorización del usuario: «go avanza», sobre la propuesta de preview técnica y producción.

## Identificación

- Sitio: `https://buscarte.com.ar`, ID Netlify `43524f56-8833-41ab-8916-cb75de47dd1e`.
- Código público: `ac97d2d9986540c3f85d21eafa3427cfc43a1886`, rama `codex/anuncios-mobile-20260905`.
- Preview: `6a9cbb6d86e46c3e7bc0ebe3`, contexto remoto `deploy-preview`.
- Producción: `6a9cbd37518d43630307438c`, verificado activo, `ready`, contexto `production`.
- Versión anterior conservada: `6a9aaf1d3f91b32a6e3336bc`, «Fix mobile header overlap», todavía `ready`.
- Caché servida: `buscarte-v9-2026-09-05-anuncios-mobile`.

[Logs de producción](https://app.netlify.com/projects/rococo-gaufre-c6b9b1/deploys/6a9cbd37518d43630307438c).

Netlify no asoció un `commit_ref` al deploy manual; el mensaje de publicación identifica el commit y la preview. El repositorio GitHub no recibió push: no desplegar su `main` anterior sin integrar esta base reconciliada. La carpeta canónica continúa siendo `BuscARTE-web`; no se modificó el proyecto Android ni el repositorio original.

## Ejecución y contexto

Netlify CLI `27.5.0`, ejecutado con Node `24.19.0` mediante el runtime existente de esta PC. Se enlazó la carpeta canónica al sitio existente; no se creó otro sitio ni se cambiaron variables, DNS o configuración remota del proyecto.

Preview:

```sh
netlify deploy --context production --dir dist --functions netlify/functions --message "Captcha + anuncios mobile ac97d2d - candidato verificado" --json
```

Importante: `--context production` selecciona configuración/variables durante el build del CLI, pero el draft creado quedó con contexto remoto `deploy-preview`. Por eso no se promovió con `restoreSiteDeploy`, cuya documentación no garantiza reasignar el contexto o las variables.

Producción desde el build ya verificado:

```sh
netlify deploy --prod --no-build --dir dist --functions netlify/functions --message "Release captcha + anuncios mobile ac97d2d - preview 6a9cbb6d validada" --json
```

El CLI rechazó un primer intento que combinaba `--context` y `--no-build`, antes de desplegar. Se comprobó que producción seguía sin cambiar antes de ejecutar el comando válido anterior. No se reconstruyeron ni modificaron los archivos públicos entre preview y producción.

La preview empaquetó las funciones con nuevos hashes. **La producción final conserva exactamente los cuatro hashes de la producción anterior**, no los de la preview. Fuentes sin cambios, runtime `nodejs24.x` y dos horarios idénticos. El `NODE_VERSION=20` histórico del TOML no se modificó.

| Función | Hash de producción conservado | Horario UTC |
| --- | --- | --- |
| recordatorio-perfil | `9bb512cfd1d732fcb217e47a08b486fd6230681c060bfae43579fef62a37d42b` | `0 13 * * 1` |
| reset-password | `93ece7099dd5df6485983aaaa0bce7a2301bdf691b5deccdc80bb2baec7c7bc1` | — |
| resumen-mensual | `62100fbf71e00a910845eba74ea080fb1545978f591cefeeac9301c0777611a8` | `0 13 1 * *` |
| send-email | `1c9c2ff51c6f71c987aa258039c59e436557c6f632b63566852b7588dfcb6c7f` | — |

## Verificación realizada

- Base local: 63 pruebas aprobadas (15 empaquetado, 17 captcha, 31 anuncios); se repitieron las 15 de empaquetado antes de subir. No hubo modificaciones funcionales después de esas pruebas.
- Preview: cuatro pruebas de navegador aprobadas sobre HTML/JS remotos, sin errores inesperados.
- Producción: mismas cuatro pruebas aprobadas sobre `https://buscarte.com.ar`, también sin errores inesperados. Se repitieron para obtener capturas.
- En ambos orígenes se verificaron los 33 recursos servidos: 14 coinciden byte por byte y 19 HTML coinciden tras normalizar exclusivamente los enlaces Pretty URLs y la serialización HTML inerte. No se ejecuta el HTML en ese comparador; se bloquea toda red del parser. JS, imágenes, manifest, assetlinks y service worker se comparan exactamente.
- Las 35 rutas llegan a 200 sin bucles. Los alias conservan sus redirecciones; las rutas que coinciden con archivos estáticos pueden responder 200 directamente porque las reglas históricas no son forzadas.
- README, documento QA, package.json y script de build responden 404; no se publicaron herramientas ni documentos internos. El inventario remoto contiene los 33 recursos y el TOML generado por Netlify; `_redirects` se consume como configuración. Los 33 hashes de fuente del inventario remoto coinciden exactamente con `dist/`, sin archivos extra fuera de esa lista y el TOML generado.
- Metadata: cuatro funciones, runtime 24, dos horarios, 35 reglas procesadas, sin nuevos headers ni Edge Functions. Una revisión independiente confirmó el sitio activo y la disponibilidad del deploy anterior.

Las pruebas de navegador sólo permiten GET estáticos del origen elegido. Supabase, RPC de vencimiento, registro, publicación, Storage, tracking, captcha y fuentes externas quedan simulados o bloqueados. No se crearon cuentas/anuncios ni se enviaron emails reales; tampoco se invocaron manualmente las funciones programadas.

## Evidencia local

Todo fuera de `dist/`, bajo `C:\Users\Usuario\Documents\ChatGPT\BuscARTE-resguardos`:

- `anuncios-20260905/preview-files-6a9cbb6d.json`.
- `anuncios-20260905/production-files-6a9cbd37.json`.
- `deploy-smoke-preview-20260905/deploy-smoke.json`.
- `deploy-smoke-production-20260905/deploy-smoke.json` y cuatro capturas. Captcha y confirmación de anuncio en estas capturas son simulados.
- Bundle completo del código previo: `anuncios-20260905/anuncios-web-history.bundle`.
- Bundle de cierre con diagnósticos/documentación: `release-20260905/release-web-history.bundle`.

Los respaldos están en esta misma PC; no sustituyen una copia externa ni un backup de Supabase.

## Reversión y límites

No se ejecutó una reversión. Si aparece una regresión atribuible a esta publicación, primero comprobar que el deploy activo sigue siendo el de este informe; no pisar un despliegue posterior de otra persona. El punto anterior verificado es `6a9aaf1d3f91b32a6e3336bc`. La operación de reversión, dentro del alcance acordado, es `restoreSiteDeploy` con el ID del sitio y ese ID de deploy. Después verificar dominio, funciones y caché: no restaurar ni modificar la base de datos como parte de una reversión web.

No se validaron dispositivos físicos ni una publicación/alta real. La app Android es una TWA del dominio de producción: ya puede recibir estos cambios web, pero falta comprobarla físicamente después de actualizar/reabrir. No hubo recompilación ni cambios de manifest, assetlinks, paquete o firma. Sigue separado el pendiente preexistente de validación antispam en servidor documentado en `qa-captcha-20260905.md`.

Referencias de plataforma: [CLI deploy](https://cli.netlify.com/commands/deploy/), [configuración de funciones](https://docs.netlify.com/build/functions/configuration/), [restoreSiteDeploy](https://open-api.netlify.com/#operation/restoreSiteDeploy).
